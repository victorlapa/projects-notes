{
  "name": "billor",
  "version": "1.0.0",
  "description": "Projects & Notes app with server-driven behaviors",
  "scripts": {
    "bootstrap": "docker compose down -v && docker compose up -d --build",
    "dev": "docker compose up --build",
    "stop": "docker compose down",
    "clean": "docker compose down -v && docker system prune -f"
  },
  "private": true
}
